class Student {
  constructor(id, firstName, lastName, grades, classRoom) {
    this.id = id;
    this.firstName = firstName;
    this.lastName = lastName;
    this.grades = grades;
    this.classRoom = classRoom;
  }
}

module.exports = Student;
